import React from 'react';
import { Image, StyleSheet, Text, View } from "react-native";

const GridPostItem = ({posts}) => {
    return (
        <Text>post</Text>
    );
};

export default GridPostItem;